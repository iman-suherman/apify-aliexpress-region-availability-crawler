{
  "title": "Input required to run",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "entries": {
      "title": "Per-country product URLs",
      "type": "array",
      "description": "One entry per country. Each entry: country code (AU, US, UK, DE, etc.) and the full AliExpress product page URL to check.",
      "editor": "json"
    }
  },
  "required": ["entries"]
}
