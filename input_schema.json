{
  "title": "Input required to run",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "productId": {
      "title": "Product ID",
      "type": "string",
      "description": "Required. AliExpress product ID from the item URL: .../item/XXXXXXXX.html → enter the number (e.g. 1005005678912345).",
      "editor": "textfield"
    },
    "countries": {
      "title": "Destination countries (region list)",
      "type": "array",
      "description": "Required. Country codes to check shipping to, one per line (e.g. AU, DE, UK, US).",
      "editor": "stringList",
      "placeholderValue": "AU"
    }
  },
  "required": ["productId", "countries"]
}
